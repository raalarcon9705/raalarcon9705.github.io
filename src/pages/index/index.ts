import {
  createParticles,
  createTextParticles,
} from '../../features/particles/index';

import { splitLetters } from '../../utils/dom';

createParticles();
createTextParticles('Hi');
splitLetters();
